spring:
  application:
    name: stream-example-003
  kafka:
    bootstrap-servers: 10.12.52.21:9092
  cloud:
    stream:
      bindings:
        myInput:
          destination: test3   # 指定topic
        myOutput:
          destination: test3
server:
  port: 2003
